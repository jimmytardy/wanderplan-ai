# Base de données PostgreSQL
# Pour Docker, utilisez les noms de services: postgres, redis
DATABASE_URL="postgresql://postgres:postgres@postgres:5432/voyage_generator?schema=public"

# Configuration PostgreSQL (pour docker-compose)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=voyage_generator
POSTGRES_PORT=5432

# JWT Secret (changez en production !)
JWT_SECRET="your-super-secret-jwt-key-change-in-production"
JWT_EXPIRES_IN="7d"

# OpenAI API
OPENAI_API_KEY="sk-..."
OPENAI_MODEL="gpt-4-turbo-preview"

# Redis (optionnel - laissez vide si non utilisé)
# Pour Docker, utilisez: redis://redis:6379
REDIS_URL="redis://redis:6379"
REDIS_PORT=6379

# Application
APP_PORT=3000
NODE_ENV="development"

# Next.js
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Optionnel: Seed la base de données au démarrage
SEED_DB=false
